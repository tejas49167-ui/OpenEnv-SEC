# Copy to .env locally if your tooling loads it (do not commit .env).
# Hugging Face Spaces: set these in the Space Settings → Secrets, not in the repo.

# API_BASE_URL=https://router.huggingface.co/v1
# MODEL_NAME=gpt-4o-mini
# HF_TOKEN=

# Quickstart

For the published docs entrypoint, see [docs/source/quickstart.md](docs/source/quickstart.md).

The fastest local flow is:

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -e ".[dev]"
sec-openenv serve
```

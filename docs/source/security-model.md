# Security Model

See the full reference at [`docs/security-model.md`](../security-model.md).

The current repository is a simulation framework, not a live security control plane. Its security model focuses on safe local execution, secret handling, deterministic offline data, and clear trust boundaries.
